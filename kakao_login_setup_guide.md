# 🟡 카카오 소셜 로그인 연동 가이드 (Supabase)

카카오 로그인을 앱에 연동하려면 **1) 카카오 디벨로퍼스(Kakao Developers) 설정**과 **2) Supabase 설정**을 오가며 작업해야 합니다. 
창을 2개 띄워두고 아래 순서대로 천천히 따라와 주세요!

---

## 1단계: Supabase에서 콜백 URL(Redirect URI) 확인하기

먼저 카카오 쪽에 "로그인 성공 시 여기로 정보를 보내줘!" 라고 알려줄 주소(Callback URL)를 확인해야 합니다.

1. **Supabase 대시보드** 접속 -> 사용 중인 프로젝트 클릭
2. 좌측 메뉴에서 톱니바퀴 모양 아래에 있는 **Authentication(인증)** 아이콘 클릭
3. Authentication 하위 메뉴 중 **Providers(제공업체)** 클릭
4. 수많은 소셜 로그인 목록 중 **Kakao**를 찾아서 클릭
5. 패널이 열리면 하단 쪽에 **`Callback URL (for OAuth)`** 이라고 적힌 주소를 복사해 둡니다.
   *(예: `https://abcd...xyz.supabase.co/auth/v1/callback`)*

---

## 2단계: 카카오 디벨로퍼스 앱 생성하기

이제 카카오 개발자 센터에서 우리 앱을 등록할 차례입니다.

1. [카카오 디벨로퍼스(Kakao Developers)](https://developers.kakao.com)에 접속 후 우측 상단의 **로그인** 클릭
2. 상단 메뉴에서 **[내 애플리케이션]** -> **[애플리케이션 추가하기]** 클릭
3. 팝업창에 정보 입력:
   * **앱 이름**: 현장일지 (또는 원하는 앱 이름)
   * **사업자명**: 본인 이름이나 팀 이름
   * 카테고리 선택 후 **[저장]**

---

## 3단계: 카카오 로그인 활성화 및 Redirect URI 등록

생성된 앱(현장일지)을 클릭하여 설정 화면으로 들어갑니다.

1. 좌측 메뉴에서 **[카카오 로그인]** 클릭
2. **"활성화 설정"** 상태를 `OFF` -> **`ON`** 으로 변경합니다.
3. 스크롤을 살짝 내려 **"Redirect URI"** 항목의 **[등록]** 버튼을 클릭합니다.
4. 아까 **1단계에서 복사해 둔 Supabase의 Callback URL**을 붙여넣고 저장합니다.

---

## 4단계: 동의 항목(이메일, 닉네임 등) 설정하기

유저가 카카오로 로그인할 때 어떤 정보를 가져올지 설정해야 합니다.

1. 좌측 메뉴에서 **[카카오 로그인]** 하위의 **[동의항목]** 클릭
2. **닉네임 (profile_nickname)**: [설정] -> **필수 동의** 선택 후 저장
3. **카카오계정 (이메일)**: [설정] -> **필수 동의** 선택 후 저장
   > [!WARNING]
   > 이메일을 "필수 동의"로 설정하려면 카카오 비즈 앱 전환(사업자 등록증 등)이 필요할 수 있습니다. 개인 프로젝트라면 "선택 동의"로 하셔도 작동합니다. (단, 앱 내에서 이메일 식별이 필요하므로 꼭 체크해 주세요)

---

## 5단계: 보안 키(Client ID, Secret) 발급받기

이제 Supabase에 입력할 열쇠(API Key)를 챙길 차례입니다.

1. 좌측 메뉴에서 **[앱 키]** 클릭
2. 4가지 키 중에서 **"REST API 키"**를 복사합니다. 👉 **(이것이 Client ID가 됩니다)**
3. 좌측 메뉴 하단의 **[카카오 로그인]** -> **[보안]** 클릭
4. **"Client Secret"** 항목에서 **[코드 생성]** 버튼을 누릅니다.
5. "활성화 상태"를 **`ON`**으로 바꾸고, 생성된 복잡한 코드를 복사합니다. 👉 **(이것이 Client Secret이 됩니다)**

---

## 6단계: Supabase에 키 입력하고 최종 마무리!

다시 **Supabase 대시보드 (1단계 화면)** 로 돌아옵니다.

1. **Kakao** 패널이 열려있는 상태에서 가장 위에 있는 **"Enable Kakao"** 스위치를 켭니다.
2. **Client ID** 칸에 아까 5단계에서 복사한 **REST API 키**를 붙여넣습니다.
3. **Client Secret** 칸에 아까 5단계에서 복사한 **Client Secret 코드**를 붙여넣습니다.
4. 하단의 초록색 **[Save]** 버튼을 클릭합니다!

🎉 **끝입니다!** 🎉
이제 앱에서 **[카카오로 시작하기]** 버튼을 누르면 정상적으로 카카오 로그인 화면이 뜨고, 로그인 성공 시 앱으로 연동됩니다! 성공하셨다면 네이버나 구글 설정 방법도 안내해 드릴 테니 편하게 말씀해 주세요!
